// lib/pages/home/widget/scheduled.dart

import 'package:flutter/material.dart';

class ScheduledBlogsGrid extends StatelessWidget {
  const ScheduledBlogsGrid({super.key});

  @override
  Widget build(BuildContext context) {
    // Replace this with your UI for scheduled blogs when you have data
    return const Center(
      child: Text(
        "You have no scheduled blogs.",
        style: TextStyle(color: Colors.grey),
      ),
    );
  }
}